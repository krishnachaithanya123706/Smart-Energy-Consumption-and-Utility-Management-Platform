from flask import Blueprint

users_bp = Blueprint("users", __name__)

@users_bp.route("/users")
def users():
    return {"message": "User management API"}
from flask import Blueprint

consumption_bp = Blueprint("consumption", __name__)

@consumption_bp.route("/consumption")
def consumption():
    return {"message": "Consumption API"}
from flask import Blueprint

anomaly_bp = Blueprint("anomaly", __name__)

@anomaly_bp.route("/anomaly")
def anomaly():
    return {"message": "Anomaly detection API"}